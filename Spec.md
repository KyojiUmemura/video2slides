# 動画からスライドPDFを自動生成するツール

## 目的

講義・プレゼンテーションを録画した動画から、表示されているスライドを自動的に抽出し、重複したスライドを除去して、1ページにつき1スライドのPDFを生成するPythonツールを作成する。

対象となる動画は、PowerPointやKeynoteなどのスライド画面を録画したものを想定する。

音声は不要であり、最終成果物には含めない。

最終的には、動画を再生する代わりに、PDFビューア上で利用者が自分のタイミングで1ページずつスライドを進められるようにする。

---

## 基本的な利用方法

コマンドラインから次のように実行できるようにする。

```bash
python video2slides.py input.mp4
```

実行すると、

```text
input.pdf
```

を生成する。

必要に応じて、中間生成されたスライド画像も保存できるようにする。

例：

```text
output/
  slide_0001.jpg
  slide_0002.jpg
  slide_0003.jpg
  ...
  slides.pdf
```

---

# 処理の基本方針

処理は概ね以下の段階に分ける。

```text
動画
 ↓
候補フレームの抽出
 ↓
スライド変更の検出
 ↓
画面が安定したフレームを選択
 ↓
類似・重複スライドの除去
 ↓
スライド画像
 ↓
PDF生成
```

---

# 1. 候補フレームの取得

FFmpegを使用して動画を読み込む。

全フレームをPythonに読み込む必要はない。

例えば10秒に2回程度など、一定間隔でフレームを取得する方法を基本とする。

ただし、短時間だけ表示されるスライドを取りこぼさないように、サンプリング間隔はコマンドラインオプションで変更可能にする。

例：

```bash
--sample-interval 4.0
```

デフォルト値：

```text
2秒
```

さらに，スライド主体のフレームだけを選ぶ。

スライド主体は，スライドの背景色が単色であることを仮定して，
ほぼ同一の色の場所が約２０％以上あることでおこなう。
比率は厳格でなくてよいので，１０００点ほどをランダムにサンプリングして
処理して良い。

これは仮定であるので，オプションでこの選択のon/offを制御できる。

```bash
--background-color-detection on
```

デフォルト値：

```text
off
```


---

# 2. スライド変更の検出

連続する候補フレームを比較し、画面が十分変化した場合に新しいスライド候補と判断する。

単純なファイルサイズや完全一致では判定しない。

以下のような画像類似度手法を検討する。

- perceptual hash（pHash）
- SSIM
- OpenCVによる画像差分

実装の複雑さと精度を考慮して適切な方法を選択する。

類似度閾値はコマンドラインオプションで変更可能にする。

```bash
--similarity-threshold 0.0005
```

デフォルト値：

```text
0.0005
```

---

# 3. 小さな変化を無視する

以下のような変化は、原則として新しいスライドとは判定しない。

- マウスポインタの移動
- レーザーポインタ
- 動画圧縮によるノイズ
- 数ピクセル程度の変化
- 微小な画面の揺れ

したがって、完全一致ではなく、画像全体としての類似度を使って判定する。

---

# 4. スライド切替途中の画像を避ける

フェード、ワイプなどのスライド切替アニメーションの途中画像を最終スライドとして採用しない。

画面が大きく変化した直後はすぐに画像を保存せず、

```text
変化
 ↓
一定時間待つ
 ↓
画面が安定
 ↓
代表フレーム取得
```

という処理を行う。

デフォルトでは変更検出後、

```text
0.7秒
```

程度経過した安定フレームを採用する。

この値も調整可能にする。

```bash
--settle-time 3.0
```

デフォルト値：

```text
0.7秒
```

---

# 5. 重複スライドの除去

連続して発生したほぼ同一のフレームは、スライド切替として扱わず1枚にまとめる。

`--dedup-mode` は、スライド切替後の安定した候補と、直前に保存したスライドの扱いを選択する。

- `keep`：安定した候補を保存する（デフォルト）
- `remove`：候補が直前に保存したスライドと同一であれば保存しない

過去に保存したすべてのスライドとの照合は行わない。したがって、

```text
slide A
slide B
slide A
```

のように時間を置いて以前のスライドへ戻った場合、後の slide A は `keep` / `remove` のどちらでも別ページとして残る。

---

---

# 8. 出力画像

スライド画像は基本的に元動画の解像度を維持する。

不必要な再圧縮によって文字が読みにくくならないように注意する。

JPEGを使用する場合は高品質設定にする。

可能であればPNGとJPEGを選択できるようにする。

例：

```bash
--image-format jpg
--jpeg-quality 90
```

デフォルト値：

```text
image-format: jpg
jpeg-quality: 95
```

---

# 9. PDF生成

抽出した画像を時系列順に並べ、1画像＝1ページとしてPDFにする。

ページサイズは画像のアスペクト比に合わせる。

16:9の動画であれば、PDFページも16:9になることが望ましい。

スライド画像をPDF化する際に、不要な余白を追加しない。

---

# 10. 背景除去（`--clean on`）

`--clean on` を指定した場合、生成されたスライド PDF に対して背景除去（透かし・色付き背景の除去）を自動で実行する。

## 処理パイプライン

```text
スライド PDF
 ↓
全ページを内部画像として読み込み（72 PPI）
 ↓
全ページ共通の背景バイアスを推定
 ↓
乗算モデルで各ページを補正（ホワイトバランス）
 ↓
背景除去後の PDF を保存
```

## 背景推定

PDF の先頭から最大 60 ページを文書順にサンプリングし、各ピクセル位置の値ヒストグラムから `--percentile` 番目の値（デフォルト p90）を計算する。このサンプルで共通して明るい成分が背景バイアスとして推定される。

- **`--bg-pages`** — 背景推定に使用する、PDF 先頭からのページ数（デフォルト: `60`）。値を大きくするほど処理時間とメモリ使用量が増える。
- **`--percentile`** — 背景推定のパーセンタイル（デフォルト: `90.0`）。
  - `90`（デフォルト）: 標準的な透かし除去
  - `95`: より保守的な除去（背景が残る方向）
  - `85`: より積極的な除去（背景が強く消える）

## 背景除去（乗算モデル / ホワイトバランス補正）

推定した背景バイアスを使って各ページを補正する：

```text
補正係数 = (255 / 背景)^intensity
出力 = clip(入力 × 補正係数, 0, 255)
```

背景が明るい領域は白に近づき、背景が濃い領域は相対的に明るくなる。暗いコンテンツ（文字、線）は乗算モデルにより保持される。

- **`--intensity`** — 背景除去の強度（デフォルト: `1.0`）。
  - `1.0`（デフォルト）: 背景を完全に白飛ばし
  - `0.5`: 背景の半分だけ白飛ばし
  - `0.0`: 除去しない（デバッグ用）

## 出力ファイル

`--clean on` 指定時、以下のファイルが生成される：

| ファイル | 説明 | 条件 |
|---------|------|------|
| `<stem>.pdf` | 背景除去後の PDF（最終結果） | 常時生成 |
| `<stem>_original.pdf` | 除去前の PDF（バックアップ） | `--clean on` 時、`<stem>.pdf` が既存の場合 |
| `<stem>_background.png` | 推定した背景マップ | `--background` 指定時 |
| `<stem>_sample.png` | 元画像・背景マップ・除去後の比較シート | `--quick-sample` 指定時 |

`<stem>` は入力ファイル名から拡張子を除いた部分。`<stem>.pdf` が既に存在する場合、背景除去前に `<stem>_original.pdf` へバックアップし、除去後の PDF を `<stem>.pdf` として上書きする。

## PDF 圧縮

- **`--compression zip|jpeg`** — PDF への圧縮方式（デフォルト: `zip`）。
  - `zip`: lossless（PNG相当）。画質劣化なし。
  - `jpeg`: lossy（JPEG相当）。ファイルサイズを小さくできる。
- **`--jpeg-quality`** — `--compression jpeg` 指定時の JPEG 品質（1-100、デフォルト: `90`）。

## 処理解像度

背景除去で PDF ページを内部画像に変換する際の解像度は、72 pixels per inch（PPI）で固定する。利用者向けの解像度指定オプションは設けない。

---

# 10. CLI

最低限、次のオプションを実装する。

```text
python video2slides.py INPUT

--output OUTPUT.pdf
--sample-interval FLOAT
--settle-time FLOAT
--similarity-threshold FLOAT
--crop x,y,width,height
--background-color-detection on|off
--dedup-mode keep|remove
--clean on|off
--bg-pages INT
--percentile FLOAT
--intensity FLOAT
--image-format jpg|png
--jpeg-quality INT
--background                     推定した背景マップを {stem}_background.png に保存
--quick-sample                   元画像・背景マップ・除去後の比較シートを {stem}_sample.png に保存
--verbose
--debug
--overwrite
--version                        バージョン表示（YY.MM.DD 形式、README.md に固定）
```

数値オプションの有効範囲：

| オプション | 有効範囲 |
|---|---|
| `--sample-interval` | 0 より大きい有限値 |
| `--settle-time` | 0 以上の有限値 |
| `--similarity-threshold` | 0.0〜1.0（両端を含む） |
| `--bg-pages` | 1 以上の整数 |
| `--percentile` | 0.0〜100.0（両端を含む） |
| `--intensity` | 0.0〜1.0（両端を含む） |
| `--jpeg-quality` | 1〜100（両端を含む整数） |
| `--crop` | `x,y >= 0`、`width,height > 0`、かつ動画フレーム内 |

浮動小数点オプションは `NaN` と無限大を受け付けない。範囲外の値はフレーム抽出や PDF 生成を開始する前にエラーとする。

例：

```bash
python video2slides.py lecture.mp4 \
    --output lecture_slides.pdf \
    --sample-interval 0.5 \
    --settle-time 0.7
```

---

# 11. 進捗表示

2時間以上の動画を処理する可能性があるため、処理状況を表示する。

例：

```text
Analyzing video: lecture.mp4

Duration: 01:52:34

[██████████████--------] 68%

Candidates detected: 83
Slides accepted: 57
Duplicates rejected: 26
```

進捗状況の報告の対象となるのは，下記の処理である。

1 Slide Frame の判定，抽出
2 Slide.pdfの生成
3 背景画像の特定
4 背景消去済みのpdfの生成

可能であれば `tqdm` を使用する。

---

# 12. FFmpeg

FFmpeg / ffprobe がインストールされていることを前提としてよい。

プログラム開始時に、

```bash
ffmpeg
ffprobe
```

が利用可能か確認する。

存在しない場合は、分かりやすいエラーメッセージを表示する。

macOSではHomebrewの場合、

```bash
brew install ffmpeg
```

でインストール可能である旨をREADMEに記載する。

---

# 13. Python環境

macOSでの利用を第一に想定する。

Python 3.11以降を対象とする。

依存パッケージは必要最低限にする。

候補：

```text
opencv-python
Pillow
imagehash
tqdm
img2pdf
PyMuPDF
numpy
```

必要性を検討し、不要な依存ライブラリは追加しない。

`requirements.txt` または `pyproject.toml` を作成する。

---

# 14. インストール

`pip` でインストール可能にし、他環境でも利用できるようにする。

```bash
# リポジトリから直接インストール
pip install git+https://github.com/umekichi/SlideVideo.git

# またはローカルからインストール
cd SlideVideo
pip install .
```

インストール後、`video2slides` コマンドが利用可能になる。

開発時には `pip install -e .` でエディタブルインストールする。

`pyproject.toml` に `[project.scripts]` でエントリポイントを設定する。

```toml
[project.scripts]
video2slides = "video2slides.cli:main"
```

---

# 15. プロジェクト構成

保守しやすいように、処理をすべて1つの巨大な関数に書かない。

初期構成として例えば以下を検討する。

```text
video2slides/
├── README.md
├── pyproject.toml
├── requirements.txt
├── video2slides.py
├── src/
│   └── video2slides/
│       ├── __init__.py
│       ├── cli.py
│       ├── video.py
│       ├── detector.py
│       ├── similarity.py
│       └── pdf.py
└── tests/
```

ただし、過度に複雑なアーキテクチャにはしない。

---

# 16. デバッグ用機能

スライド検出アルゴリズムを調整しやすくする。

`--verbose` または `--debug` を指定した場合、

```text
00:01:24.5 difference=0.012 SAME
00:01:25.0 difference=0.014 SAME
00:01:25.5 difference=0.482 CHANGE
00:01:26.0 difference=0.122 TRANSITION
00:01:26.5 difference=0.018 STABLE -> SAVE
```

のような情報を表示する。

可能なら、

```text
debug/
```

ディレクトリに判定候補画像を保存する。

---

# 17. 安全性

元動画は絶対に変更・上書きしない。

出力先に同名ファイルが存在する場合は、意図せず上書きしないようにする。

エラーが発生した場合、中途半端なPDFを最終成果物として残さない。

---

# 18. README

READMEには最低限以下を記載する。

- ツールの目的
- macOSでのセットアップ
- FFmpegのインストール
- Python環境の構築
- 基本的な使い方
- CLIオプション
- スライド検出の考え方
- 推奨パラメータ
- トラブルシューティング

---

# 19. テスト

最低限、画像類似度判定についてユニットテストを書く。

可能なら短いテスト動画を人工的に生成し、

```text
slide A 10秒
slide B 10秒
slide C 10秒
```

から3ページのPDFが生成されることを確認するintegration testを作る。

テスト素材を巨大なバイナリファイルとしてGitリポジトリに含める必要はない。

FFmpegを使ってテスト時に生成してよい。

---

# 20. 実装の優先順位

まず、シンプルで確実に動くMVPを完成させる。

MVPでは以下を必須とする。

1. 動画ファイルを受け取る
2. 一定間隔でフレームを解析する
3. スライドの変化を検出する
4. ほぼ同じ連続フレームを除去する
5. 安定した代表フレームを保存する
6. PDFを生成する
7. CLIとして利用できる
8. READMEを作る

Progressive Revealの統合やスライド領域の自動検出など高度な機能は、MVP完成後に追加する。

---

# 21. 実装時の方針

実装を開始する前に、まずこの仕様を確認し、

1. 採用するスライド検出アルゴリズム
2. 使用するPythonライブラリ
3. プロジェクト構成

を簡潔に説明する。

その後、実際にコードを実装する。

実装後は実際にテストを実行し、エラーがあれば修正する。

READMEに書くだけで終わらせず、可能な範囲で実際に動作確認する。

過度な抽象化や将来を見越した複雑な設計よりも、

**読みやすく、調整しやすく、実際に動くコード**

を優先する。
